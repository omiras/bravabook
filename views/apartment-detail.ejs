<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle del apartamento</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">

</head>
<body>
    <main class="container">
        <section>
            <header><%= apartment.title %></header>
            <article><img src="<%= apartment.mainPhoto %>" alt=""></article>
            <footer><%= apartment.price %> € | <%= apartment.squareMeters %> m<sup>2</sup> </footer>
            <a href="/admin/apartment/<%= apartment._id %>/edit">Editar</a>
        </section>
    </main>
</body>
</html>